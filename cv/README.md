# Testing 
Run the development server:

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to view the website.

# Export as a static website

Run build

```bash
npm run build
```

Static files can be served from /cv/out